return {
  preset = "modern",
  options = {
    show_source = {
      enabled = true,
      if_many = true,
    },

    multilines = {
      enabled = true,
      always_show = false,
    },

    show_diags_only_under_cursor = false,

    override_open_float = true,
  },
}
